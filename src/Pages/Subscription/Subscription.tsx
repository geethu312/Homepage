import SideBar from "../../Components/SideBar/SideBar";
import "./Subscription.scss";

const Subscription = () => {
  return (
    <div>
      <SideBar />
    </div>
  );
};

export default Subscription;
